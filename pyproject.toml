[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mdfarmer"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["mdfarmer"]
package-dir = {"mdfarmer" = "."}
